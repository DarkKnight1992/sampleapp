import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  innerStyle: {
    flex: 1,
    flexBasis: '100%',
    flexGrow: 1,
    flexShrink: 0,
    width: '100%',
    height: '100%',
  },
});

export default styles;
